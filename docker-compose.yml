version: '3.8'

services:
  backend:
    build: ./backend
    container_name: shooper-backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: shooper-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
